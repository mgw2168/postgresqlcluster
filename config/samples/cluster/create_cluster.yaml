apiVersion: pgcluster.kubesphere.io/v1alpha1
kind: PostgreSQLCluster
metadata:
  name: postgresqlcluster-test
  namespace: pgtest
spec:
  clientVersion: "4.7.1"
  name: "test001"
  namespace: "pg-cluster"
  syncReplication: true
  ccpImage: ""
  ccpImageTag: ""
  replicaCount: 1
  cpuLimit: "200m"
  cpuRequest: "200m"
  memoryLimit: "1Gi"
  memoryRequest: "1Gi"
  database: "db1"
  username: "pguser1"
  password: "pass1"
  # restart
  rollingUpdate: true
#  podAnnotation:
#    - podName: "test001-kmfy"
  # delete cluster

  # scale up
  nodeLabel: ""
  serviceType: ""
  storageConfig: ""
  tolerations: []
  # scale down
  replicaName: "test001-kmfy"
  # create user
  managedUser: true
  passwordAgeDays: 86400
  passwordLength: 8
  passwordType: "md5"
#  users:
#    - username: "pgtest"
#      password: "PassWord@"
  # delete user

  # show user
  showSystemAccounts: true
  # update user
  setSystemAccountPassword: false

  # pg params configmap

